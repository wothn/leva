/**
 * 生词本扩展 - 内容脚本入口
 * 此文件作为模块化后的入口点，导入并初始化各功能模块
 */

// 导入内容脚本主模块并确保执行
import './content/main.js';

// 这一行是关键，确保模块代码被执行
console.log('生词本扩展: 内容脚本已加载');

// 注意：如果你的浏览器扩展不支持ES模块，
// 请考虑使用打包工具（如webpack）将代码打包，
// 或者使用传统的脚本引入方式